{
  "name": "ideascale-api-wrapper",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy-dev": "serverless deploy --aws-profile personal",
    "deploy-prod": "serverless deploy --stage prod --aws-profile personal"
  },
  "dependencies": {
    "node-fetch": "^2.6.1"
  },
  "devDependencies": {
    "serverless": "^2.11.1"
  }
}
